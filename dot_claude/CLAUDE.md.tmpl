{{- template "AGENTS.md" . }}

## セッション自動リネーム

Plan モードが完了したら、その内容をひと言（日本語・20文字以内）でまとめて、
最初のレスポンスの冒頭で以下のコマンドを提案してください：

/rename {要約}

例：
- `/rename 認証フローのリファクタ`
- `/rename READMEの英語化`
- `/rename APIレート制限の実装`

Plan を使わない短い依頼でも、Stop 時に適切な名
