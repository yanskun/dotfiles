#!/bin/bash
# Claude設定をシンボリックリンク
mkdir -p "$HOME/.claude"
ln -sf "{{ .dotdir }}/claude/settings.json" "$HOME/.claude/settings.json"
ln -sf "{{ .dotdir }}/claude/commands" "$HOME/.claude/commands"
ln -sf "{{ .dotdir }}/claude/agents" "$HOME/.claude/agents"
ln -sf "{{ .dotdir }}/claude/CLAUDE.md" "$HOME/.claude/CLAUDE.md"
