#!/bin/bash
# Gemini設定をシンボリックリンク
mkdir -p "$HOME/.gemini"
rm -f "$HOME/.gemini/settings.json"
ln -sf "{{ .dotdir }}/gemini/settings.json" "$HOME/.gemini/settings.json"
ln -sf "{{ .dotdir }}/gemini/commands" "$HOME/.gemini/commands"
ln -sf "{{ .dotdir }}/gemini/skills" "$HOME/.gemini/skills"
